---
- include: locale.yaml
  tags:
    - locale

- include: packages.yaml
  tags:
    - packages

- include: network.yaml
  tags:
    - network

- include: user.yaml
  tags:
    - user

- include: rsyslog.yaml
  tags:
    - rsyslog-clients

- include: firewall_rules.yaml
  tags:
    - firewall

- include: packages.yaml

- include: network.yaml

- include: user.yaml

- include: timesyncd.yaml

- include: freeipa_enroll.yml
# - name: Show facts available on the system
#   ansible.builtin.debug:
#     var: ansible_facts
